import RegisterForm from '@/auth/register/RegisterForm';
import React from 'react';

const page = () => {
    return (
        <div>
            <RegisterForm/>
        </div>
    );
};

export default page;