import SettingsHome from '@/Pages/Settings/SettingsHome';
import React from 'react';

const Settings = () => {
    return (
        <div>
            <SettingsHome />
        </div>
    );
};

export default Settings;